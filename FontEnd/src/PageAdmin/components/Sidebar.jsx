import React from 'react'

function Sidebar({ type }) {
    const handleRenderManage = (type) => {
        switch (type) {
            case 'manageBrand':

                break;

            default:
                break;
        }
    }
    return (
        <div>
            
        </div>
    )
}

export default Sidebar